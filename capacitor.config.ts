import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.certifyyaself.pizzashop',
  appName: 'pizzaShop',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
